Backend for my website davidespinosa.dev

To install all of the libraries the backend needs type:
npm install

To run the unit tests:
npm run test

To run linting:
npm run lint

And for deployment you can use:
npm run deploy